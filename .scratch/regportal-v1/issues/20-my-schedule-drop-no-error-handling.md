Status: done

> *This was generated by AI during triage.*

## Bug

`drop()` and `waitlistDrop()` in `useScheduleRegistration` have no try/catch. The confirmation dialog in `ScheduleView` clears `pendingAction` before awaiting the drop, so if the network call fails: the dialog is already gone, `_applyResult` is never called (so no `sectionErrors` entry is written), and the student sees no feedback whatsoever. The failure is an unhandled promise rejection — `main.js` has no global error handler and `ScheduleView` has no `onErrorCaptured`.

## Agent Brief

**Category:** bug
**Summary:** Network errors in drop/waitlistDrop are silently swallowed — student sees no feedback

**Current behavior:**
`drop()` and `waitlistDrop()` `await registerSections(payload)` with no try/catch. `confirmAction()` in `ScheduleView` also has no catch. If the request rejects (network failure, server 500), the error propagates as an unhandled rejection. The confirmation dialog has already been dismissed. No error message appears in the UI.

**Desired behavior:**
Network and server errors during drop/waitlistDrop should surface to the student. The established pattern in this codebase is to write to `authStore.sectionErrors[courseKey]`, which ScheduleView already renders inline alongside the section row, and a Dismiss button is already wired. The error should be written there so the student knows the drop failed.

**Key interfaces:**
- `useScheduleRegistration.drop(sectionId)` and `waitlistDrop(sectionId)` — wrap the `registerSections` call in try/catch; in the catch block write to `authStore.sectionErrors[String(sectionId)]`
- `authStore.sectionErrors` — the reactive map already used for API-level errors returned in `data.rows[0].errors`; reuse it for network/server errors with a generic message
- `confirmAction()` in `ScheduleView` — optionally wrap in try/catch as a belt-and-suspenders, but the fix in the composable is the primary surface

**Acceptance criteria:**
- [ ] If `registerSections` rejects (network error), `authStore.sectionErrors[sectionId]` is set to a user-readable error message
- [ ] The inline error banner appears in the summary list for the affected section after a network failure
- [ ] The student can dismiss the error via the existing Dismiss button
- [ ] On success (no throw), behavior is unchanged — section is removed from the list
- [ ] A unit test covers the network-error path: `registerSections` rejects → `sectionErrors[id]` is set

**Out of scope:**
- Toast notifications or global error UI
- Retry logic
- Changes to `confirmAction` beyond removing the silent failure

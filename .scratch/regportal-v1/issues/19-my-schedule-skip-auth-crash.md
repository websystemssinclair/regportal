Status: done

> *This was generated by AI during triage.*

## Bug

VITE_SKIP_AUTH state branch is missing `currentCourses`, `waitlist`, `sectionErrors`, and `colleagueToken`. Any developer who opens My Schedule in local dev immediately hits a `TypeError: Cannot read properties of undefined (reading 'map')` because `ScheduleView` calls `.map()` on `authStore.currentCourses` and `authStore.waitlist` inside the `registrationTerms` computed.

## Agent Brief

**Category:** bug
**Summary:** VITE_SKIP_AUTH auth store state missing My Schedule fields causes crash on mount

**Current behavior:**
The `VITE_SKIP_AUTH` branch of `authStore`'s `state()` factory returns only `{ isAuthenticated, currentRole, user, apiKey }`. After the My Schedule work, `ScheduleView` reads `authStore.currentCourses` and `authStore.waitlist` (used in `registrationTerms`, `termCourses`, `termWaitlisted`) and `authStore.sectionErrors` (shown in the summary list). All three are `undefined` under VITE_SKIP_AUTH, causing a crash on mount.

**Desired behavior:**
The VITE_SKIP_AUTH state branch should include `currentCourses: []`, `waitlist: []`, `sectionErrors: {}`, and `colleagueToken: null` so that ScheduleView mounts cleanly and a developer can seed mock data manually if needed.

**Key interfaces:**
- `authStore` state factory — the VITE_SKIP_AUTH conditional branch
- Same three fields added by the My Schedule work to the normal branch: `currentCourses`, `waitlist`, `sectionErrors`
- `colleagueToken` is also absent from the skip-auth branch and is needed by `useScheduleRegistration._buildPayload`

**Acceptance criteria:**
- [ ] Under VITE_SKIP_AUTH=true, `authStore.currentCourses` is `[]` (not undefined)
- [ ] Under VITE_SKIP_AUTH=true, `authStore.waitlist` is `[]` (not undefined)
- [ ] Under VITE_SKIP_AUTH=true, `authStore.sectionErrors` is `{}` (not undefined)
- [ ] Under VITE_SKIP_AUTH=true, `authStore.colleagueToken` is `null` (not undefined)
- [ ] ScheduleView mounts without TypeError under VITE_SKIP_AUTH=true
- [ ] Existing VITE_SKIP_AUTH behavior (role=Developer, user stub) is unchanged

**Out of scope:**
- Seeding mock currentCourses/waitlist data for developer testing
- Changes to ScheduleView or useScheduleRegistration
